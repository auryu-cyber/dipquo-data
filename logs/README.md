# logs

ログイン履歴・操作履歴の保存場所。アプリが実行時に作成する。

- `login/YYYY-MM.jsonl` — ログイン試行(成功/失敗)。1行1イベントのJSON Lines。
- `activity/YYYY-MM.jsonl` — 操作履歴(見積の作成・編集・エクスポートなど)。1行1イベントのJSON Lines。

実データが入るまでこのディレクトリは空です(Gitは空ディレクトリを追跡しないため、`login/` `activity/` はアプリが最初のログを書き込んだ時点で作成されます)。
